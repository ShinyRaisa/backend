{
  "name": "backend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../introdemo && npm run build && cp -r dist ../backend",
    "deploy:full": "npm run build:ui  && git add . && git commit -m uibuild && git push"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ShinyRaisa/backend.git"
  },
  "author": "shiny",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/ShinyRaisa/backend/issues"
  },
  "homepage": "https://github.com/ShinyRaisa/backend#readme",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "morgan": "^1.10.1"
  }
}
